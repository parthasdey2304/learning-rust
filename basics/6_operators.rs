fn main() {
    /* there are basically 3 types of operators : 
    1. Arithmatic Operators
    2. Relational Operators
    3. Logical Operators
    */

    // list of all the arithmatics operators
    let a = 5;
    let b = 10;
    let sum = a + b;
    let difference = b - a;
    let product = a * b;
    let quotient = b / a;
    let remainder = b % a;

    // list of relational operators
    let greater_than = 5 > 3;
    let lesser_than = 3 < 5;
    let greater_than_equal_to = 6 >= 4;
    let lesser_than_equal_to = 9 <= 15;
    let is_equal_to = 9 == 9;

    // list of logical operators
    
}